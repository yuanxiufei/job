<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.reginyuan.job.mapper.JobCategoryMapper">
    <select id="allJobCategory" resultType="com.reginyuan.job.domain.JobCategory">
        SELECT *
        FROM `job_category`
    </select>
    <insert id="insertJobCategory"  parameterType="com.reginyuan.job.domain.JobCategory" useGeneratedKeys="true" keyProperty="id">
       INSERT INTO `job_category` (category_name,category_desc) VALUES (#{category_name},#{category_desc})
    </insert>

    <delete id="deleteJobCategory" parameterType="java.lang.Integer">
        DELETE FROM `job_category` WHERE id = #{id}
    </delete>
    <update id="updateByPrimaryKey"  parameterType="com.reginyuan.job.domain.JobCategory">
        UPDATE `job_category` SET  category_name = #{category_name}, category_desc = #{category_desc} WHERE id =#{id}
    </update>
</mapper>
